rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isMasterAdmin() {
      return request.auth != null && request.auth.token.email == 'tamir91441299@gmail.com';
    }

    // Users collection
    match /users/{userId} {
      allow read, write: if true;
    }

    // Notifications collection
    match /notifications/{notifId} {
      allow read, write: if true;
    }

    // Scores collection (Game & Quiz leaderboard)
    match /scores/{scoreId} {
      allow read, write: if true;
    }

    // Promo codes collection
    match /promo_codes/{codeId} {
      allow read, write: if true;
    }

    // Security Settings collection (Passcodes & global configs)
    match /settings/{settingId} {
      allow read, write: if true;
    }

    // Movies & Video episodes collection (Only admin Tamir can upload / add / edit videos)
    match /movies/{movieId} {
      allow read: if true;
      allow write: if isMasterAdmin();
    }

    // Custom video episodes collection
    match /episodes/{epId} {
      allow read: if true;
      allow write: if isMasterAdmin();
    }

    // Anime views & watchers tracking (who watched, how many people watched)
    match /anime_views/{viewId} {
      allow read, write: if true;
    }

    // Recharge requests collection (Users submit payment proof; Admin approves/rejects)
    match /recharge_requests/{reqId} {
      allow read, write: if true;
    }
  }
}

